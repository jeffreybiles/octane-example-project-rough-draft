<div class="table-cell header">
  {{#if @sortBooks}}
    <span {{action @sortBooks @sortProperty true}} class="clickable">&uarr;</span>
    <span {{action @sortBooks @sortProperty false}} class="clickable">&darr;</span>
  {{/if}}
  {{@columnName}}
</div>
