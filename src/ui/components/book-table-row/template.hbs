<div class="table-row {{if isSelected 'selected'}}" {{action toggleMySelection @book}}>
  <div class="table-cell">{{@book.name}}</div>
  <div class="table-cell">{{@book.author}}</div>
  <div class="table-cell">{{@book.publicationYear}}</div>
  <div class="table-cell">{{@book.pages}}</div>
  <div class="table-cell">
    <button {{action toggleMyBook @book preventDefault=true}}>{{@toggleText}}</button>
    {{link-to 'Visit' 'books.book' tagName="button"}}
  </div>
</div>
